#!/usr/bin/env rake
require 'bundler/gem_tasks'
require 'rake/testtask'
require 'fringe'
namespace :fringe do
  desc "shwos the current version of Frigne"
  task :version do
    puts Fringe::VERSION
  end
end
